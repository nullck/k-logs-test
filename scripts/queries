# list all indexes
curl -X GET 'http://localhost:9200/_cat/indices?v'

# list all docs in index
curl -X GET 'http://localhost:9200/sample/_search'

# query by parameters
curl -X GET http://localhost:9200/samples/_search?q=school:Harvard

# query by json
curl -XGET --header 'Content-Type: application/json' http://localhost:9200/samples/_search -d '{
      "query" : {
        "match" : { "school": "Harvard" }
    }
}'

# inform the polaris to use bulk to indexing the data
# create the proper mapping, setting the correct field as analyzed and not analyzed


# query by parameters
curl -X GET http://localhost:9200/test_logs/_search?q=kubernetes.pod_name:test-logs
